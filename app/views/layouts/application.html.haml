!!!
%html
  %head
    %title Story Mapper
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    
  %body
    #navigation
      %ul
        %li#sign_in
          - if current_user.present?
            = current_user.username
            %span.welcome
              = link_to "Sign Out", destroy_user_session_path, :method => :delete
          - else
            = link_to "Sign In", new_user_session_path
        %li.first= link_to "Projects", projects_path
        %li Categories
        %li Profile
    
    #messages
      - if notice.present?
        %p.notice.ui-state-highlight
          = notice
      
      - if alert.present?
        %p.alert.ui-state-error
          = alert
    
    #content  
      = yield
